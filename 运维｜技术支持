1、k8s的pod和docker里的container是一样的嘛 如果不一样 区别是啥?
答:
Kubernetes中的Pod是最小的可调度和可部署单元，包含一个或多个相关容器，共享同一个网络命名空间、存储卷等资源，支持本地进程间通信和文件系统共享。
Docker容器则是一个独立运行的软件包装形式，提供跨平台移植性和隔离性，每个容器都有自己的网络、进程和文件系统空间。
Pod更注重应用程序或服务之间的协作和管理，而Docker容器更侧重于单个应用程序或服务的隔离和封装。在Kubernetes中，可以使用Pod来管理和协调多个Docker容器。

2、什么是linux文件系统?
答:
Linux文件系统是将文件和目录组织成树形结构，并为其提供访问和管理的一种机制。它由引导文件系统（bootfs）和根文件系统（rootfs）两部分组成。
bootfs包含引导加载程序和内核镜像，用于启动操作系统内核；而rootfs则是Linux系统中的根文件系统，包含标准目录和文件，成为整个系统的核心。
除了常见的EXT4、Btrfs、XFS等本地文件系统外，Linux还支持使用NFS等远程文件系统进行文件共享和分布式存储。

3、linux中vim编辑保存后inode会发生变化嘛?
答:
取决于backupcopy是no还是yes,默认是no，也就是不管是否改变内容，保存就会生成新inode。
在vim .vimrc设置。

4、Linux用户想要使用网络功能
答:
Linux系统中的网络功能是通过通用的Linux网络设备实现的，例如eth0、wlan0等。这些设备由内核和驱动程序生成，
用户可以使用ifconfig、ip等命令行工具或Network Manager图形化界面对其进行配置和管理。
除了物理网卡，Linux还支持虚拟网卡、桥接设备、VPN接口等不同类型的网络设备，用户可以根据需求选择不同的设备来实现网络连接和数据传输等功能。

5、Bridge(桥)是什么?
答:
在Linux系统中，Bridge（桥）是一个设备，用于实现TCP/IP二层协议交换，类似于交换机，在多个网络接口间形成局域网。
通过检查数据包头部的MAC地址，决定是否广播、转发或丢弃该数据包，提高网络传输效率和可靠性。
管理员可以使用brctl命令创建和管理Bridge设备实例，并将其与其他网络设备进行连接，实现设备间的通信和数据转发。

6、Docker端口映射
答:
在Docker中，端口映射是将容器内的端口映射到主机上的一个或多个端口，通过-p或--publish选项实现。
Docker会自动创建一条iptables规则，将主机的指定端口映射到容器的指定端口上，实现外部网络与容器的通信。
管理员也可以手动配置iptables规则来实现端口映射，但需要一定的Linux网络知识和技能，并增加配置的复杂性和不稳定性。因此，建议在大多数情况下使用Docker提供的-p选项来实现端口映射。

7、平时一般去那里搜索技术文章?
答:
在搜索技术文章时，最常用的是Google和Bing这两个全球最大的搜索引擎，它们能够提供各种技术文章和文档，拥有高效的搜索算法和优秀的搜索结果排名。
Stack Overflow是全球最大的技术问答社区之一，在这里可以找到大量的技术文章和教程，以及解决实际问题的有效方法和思路。
中国的技术社区SegmentFault也提供了丰富的技术文章、问答和交流资源。此外，GitHub、CSDN、知乎等网站也是重要的技术资源平台。
社交媒体平台如Twitter、LinkedIn等也是许多行业专家和技术大牛分享经验和见解的重要渠道。

8、nginx java
答:
在Java应用服务器和Nginx服务器之间配合时，一般采用反向代理和负载均衡的方式来实现高效、稳定的服务。静态资源交给Nginx处理，
动态请求转发给Java应用服务器（如Tomcat）处理，利用Nginx快速响应静态请求并减轻Java服务器压力。利用Nginx自身的负载均衡功能，
将请求分发给多台Java应用服务器进行处理，提高系统的可用性和扩展性。需要注意配置和调优，并考虑安全性、缓存和日志等问题，确保系统的稳定性和安全性。

9、servlet是什么?
答:
Servlet是Java Web应用程序的组成部分，运行在Web服务器上的Java类，用于处理HTTP请求和响应。
优点包括跨平台性、动态性、高效性和可扩展性，实现个性化和定制化的Web应用程序，并提高性能和可靠性。需要遵循Java EE规范和标准，
确保在不同Servlet容器上具有一致性和互操作性，同时依赖于Java EE环境和Servlet容器的支持，需要了解和掌握相应的技术和工具。

10、tomcat
答:
Tomcat是一个常用的开源Servlet容器和Web服务器，支持Java Servlet和JSP等Web应用程序技术，并提供了丰富的功能和插件，如连接池、安全性、负载均衡、SSL等。
Tomcat是Apache组织的开源项目，最新版本是Tomcat 10。Java Servlet是用Java编写的服务器端程序，处理HTTP请求和响应，运行于Servlet容器中，例如Tomcat、Jetty、JBoss等。
JavaServerPages（JSP）是一种动态网页技术，将Java代码嵌入到HTML页面中，生成动态Web内容。
需要注意，Tomcat不仅是Servlet容器，还是独立的Web服务器，可提供高性能的Web服务，同时支持多个虚拟主机和多个Web应用程序。

11、ansible各个组件
答:
Ansible是一种自动化工具，用于配置和管理计算机环境，采用基于SSH协议的无代理架构，具有易学易用、快速部署、高效安全等特点。
主要组件包括Ansible程序、Connection plugins、Host inventory、Core modules、Custom modules、Plugins、Playbooks等。
还支持变量管理、条件控制、模板渲染等高级功能，可与其它自动化工具进行集成，实现更加强大的自动化管理能力。

12、nginx cpu mem的关系 也就是用多大的CPU和多大的内存
答:
Nginx在作为反向代理服务器时，CPU和内存的配置对性能和资源消耗有重要影响。调整Worker_processes、Worker_connections、Max_clients等参数可以优化Nginx性能，
建议将其设置为4核8GB内存，根据实际情况进行调整。同时还需注意Linux系统的文件句柄数、进程数和文件打开数等系统限制，可以使用缓存减少对后端服务器请求频率，降低资源消耗和延迟。
需要进行系统的优化和调整，以达到最佳性能和稳定性。

13、exec命令的三个场景使用
Exec是一个Unix/Linux Shell内置命令，用于执行指定的命令或重定向标准输入、输出和错误流。使用场景包括：调用并执行指定命令，重定向标准输入、输出和错误流，和find结合使用批量处理文件或目录。
需要注意exec命令会覆盖当前Shell进程，可能会影响其它进程和环境，因此使用时需谨慎。同时，exec命令也可用于编写Shell脚本和程序，实现更高效和灵活的操作。

14、云资源:服务器、交换机、路由器、防火墙等的监控协议
SNMP (Simple Network Management Protocol)：一种用于网络管理的标准协议，可监控服务器、交换机、路由器等设备。
NetFlow：一种流量分析协议，可以监控网络设备的流量，并提供关于源、目标和协议等信息的统计数据。
sFlow：一种基于流量采样的协议，可以在网络设备中实现端口级别的监控，并提供类似于NetFlow的统计数据以及更详细的信息。
IPFIX (Internet Protocol Flow Information Export)：一种基于NetFlow的流量监测协议，在IPv6环境下广泛使用。
Syslog：一种用于记录日志消息的标准协议，可监控服务器、防火墙等设备。

15、用shell脚本怎么把文件中第八行的某个单词
#!/bin/bash
# 读取文件中第八行的内容，提取需要替换的单词
word=$(sed -n '8p' filename.txt | awk '{print $2}')
# 替换单词为新的值
sed -i "s/${word}/new_word/" filename.txt

16、广播类型dis默认发送hello时间间隔是多少秒
DIS（Distributed Interactive Simulation）广播协议是一种用于分布式仿真的标准协议，它定义了在仿真中传输数据和交换消息的方式。而hello消息则是DIS协议中的一种控制消息。
根据DIS协议规范，hello消息的发送时间间隔应该是1秒钟。这个时间间隔可以通过修改DIS协议中的Heartbeat Interval参数来调整。
但是需要注意的是，在实际应用中，具体的时间间隔可能会因为网络延迟等因素而有所不同。

17、服务器遭受攻击，cpu升高，流量升高，一般会用什么工具或命令排查线索，一般如何处理。
使用top、htop等命令查看CPU占用率较高的进程，并记录这些进程的PID。
使用netstat、iftop等命令查看网络流量变化情况，并记录与服务器通信的IP地址。
使用tcpdump等抓包工具对服务器的网络流量进行抓取，以便进一步分析攻击类型和来源。
安装防火墙软件，如iptables，对入站和出站流量进行控制，阻止非法访问。
对服务器上的敏感数据进行备份，并在必要时关闭服务器。

18、磁盘空间满了，发现有个nginx.log文件超大，通过rm -f删除
磁盘空间满了，发现有个nginx.log文件超大，通过rm -f删除
使用du -h命令查看磁盘空间的使用情况，确认nginx.log文件的大小和所占用的磁盘空间。
使用tail -f命令查看nginx.log文件的最新记录，并考虑将其中的重要信息备份到其他位置。
使用cp命令将需要保留的数据保存到备份文件中。
使用echo "" > nginx.log命令清空nginx.log文件，以释放磁盘空间。
如果需要恢复nginx日志功能，可以重新启动nginx服务。

19、用过哪些软路由系统
OpenWrt：开源的嵌入式操作系统，可在各种硬件平台上运行，具有高度可定制性和灵活性。
DD-WRT：基于 Linux 的替代固件，具有类似 OpenWrt 的功能，但更注重易用性和简单性。
pfSense：具有强大的网络安全功能和易于管理的 Web 界面的开源防火墙和路由器平台。
Tomato：开源路由器固件，注重易用性和美观性，并提供了一些额外的网络功能，如带宽监视和 QoS 控制。
IPFire：适用于小型企业和家庭网络的完整 Linux 发行版，具有可扩展的插件架构和高级防火墙功能。

20、Tomcat如何对外暴露端口
http
<Connector port="8080" protocol="HTTP/1.1"
           connectionTimeout="20000"
           redirectPort="8443" />
https           
<Connector port="8443" protocol="HTTP/1.1"
           maxThreads="150" SSLEnabled="true">
    <SSLHostConfig>
        <Certificate certificateKeystoreFile="conf/localhost-rsa.jks"
                     type="RSA" />
    </SSLHostConfig>
</Connector>

21、数据包经过二层交换机转发源地址会变吗，经过三层会变吗？
数据包经过二层交换机转发时，源地址通常不会改变。这是因为二层交换机工作在数据链路层，只关注目的MAC地址并将帧转发到对应的端口，不涉及IP地址等其他协议信息。
当数据包经过三层交换机（或路由器）时，源地址通常会被改变。这是因为三层交换机工作在网络层，需要查找路由表并更改目的IP地址和源IP地址以进行正确的路由。
如果源地址不改变，则可能导致响应数据包无法返回到正确的源设备。

22、如何存储和优化过亿行的表
数据库选择与设计：选择适合数据存储和查询的数据库类型（如关系型数据库、非关系型数据库等），并进行合理的数据表设计和索引设计。可以根据需求对表进行分片，即把表按照某种规则拆分成多个子表，以减少单个表的数据量。
硬件设备配置：硬件设备的配置也是影响表存储和查询效率的关键因素。需要考虑存储设备的性能（如磁盘读写速度、IOPS等）、内存大小和CPU处理能力等。可以采用分布式存储系统或集群技术，将数据分散到多台服务器上进行存储和查询。
数据清洗和去重：在数据量比较大时，需要对数据进行清洗和去重，以减少冗余数据和无效数据对存储和查询的影响。
查询优化：针对常用的查询操作进行优化，如创建适当的索引、使用延迟加载等方式来提高查询性能。

23、运维工程师遇到的疑难问题的排查有哪些
确定问题现象:首先需要了解用户遇到的具体问题现象，包括什么时候出现、如何出现、是否有提示等等。
确认影响范围:确认问题是否只影响单个用户还是多个用户，确定影响范围可以帮助快速定位问题。
查看日志:在服务器上查看系统日志和应用程序日志，寻找相关错误信息，以便定位问题。
检查网络连接:检查网络是否正常，例如网络设备是否故障、DNS解析是否成功等等。
重启相关服务:尝试重启相关的进程或服务，以验证问题是否会消失。
进行系统监控:使用监控工具（例如Zabbix、Nagios等）对系统进行监控，以便及时发现问题。
分析代码:如果问题是由应用程序引起的，可以分析代码，找出问题所在。
查看硬件状态:

24、域渗透，内网黄金票据和白银票据区别和利用方式
域渗透
域渗透是指黑客在网络中获取足够的权限，以便可以访问和控制公司或组织内的系统和数据。在域渗透过程中，攻击者通常会使用各种工具和技术，例如钓鱼攻击、漏洞利用等来突破网络的安全防御机制。
内网黄金票据和白银票据
在域渗透中，黄金票据和白银票据是攻击者主要目标之一。这些票据是指在Windows域环境中的身份验证凭证，可以让攻击者获取对域中的所有计算机和资源的完全控制。
具体来说，黄金票据是指可以访问域控制器并进行任意操作的凭据，包括修改密码、添加用户等操作。而白银票据则是指能够访问域中其他计算机的凭据，通常由本地管理员或域管理员帐户的NTLM哈希值构成。

25、再同一个内网，对方开启了防火墙，如何渗透
使用隧道技术绕过对方防火墙并进行未经授权的访问和操作。

26、数据传输是怎么传输的，从开始到结尾的一种描述
数据源选择要发送的数据，例如一个文件或一个消息。
数据被分成较小的块（称为数据包）并添加元数据，例如源地址和目标地址。
发送方的操作系统将数据包传递给网络协议栈。
网络协议栈使用协议（例如TCP或UDP）将数据包封装在IP数据包中，并添加路由信息，使其可在网络上进行传输。
数据包沿着网络链路被路由到目标设备。
目标设备的网络协议栈解开IP数据包并根据目标端口号确定哪个应用程序将接收数据包。
应用程序接收数据包并将其还原为原始数据。
目标设备的操作系统确认数据已成功接收并发送确认给源设备。
源设备收到确认并停止发送数据。

27、Linux内核调优
调整内存参数：可以通过修改Swappiness和vm.dirty_background_ratio等参数来控制内存使用情况。
调整文件系统参数：可以通过修改inode缓存大小和文件系统块大小等参数，来优化文件系统的性能。
调整网络参数：可以通过修改TCP缓冲区大小、增加最大连接数等参数，来优化网络性能。
禁用不必要的内核模块：禁用不必要的内核模块可以减少内核启动时间和占用的内存。
启用适当的调试选项：启用适当的调试选项可以帮助诊断系统故障。
调整CPU调度器：可以通过修改sched_latency_ns和sched_min_granularity_ns等参数，来优化CPU调度器的性能。
增加文件描述符限制：可以增加文件描述符限制，来满足应用程序对文件句柄的需求

28、如何用kill一次性杀掉系统中所有java进程？
#!/bin/bash
jps | awk '/[A]pplication/ {print $1}' | xargs -I {} sh -c 'kill -15 {}'

29、前端vue写的代码如何做UI测试
1）学习官方文档：Vue.js官方文档提供了详细的关于Vue组件单元测试的教程和方法。
2）安装Vue Test Utils：Vue Test Utils是Vue.js官方的单元测试实用工具库，可以通过安装来使用。
3）选择一个测试运行器：为了运行单元测试，您需要选择一个测试运行器。常见的测试运行器有Jest、Mocha和Karma。
4）编写测试用例：在进行UI测试时，需要针对每个Vue组件编写测试用例。
5）运行测试：使用所选的测试运行器运行编写好的测试用例，查看测试结果。

30、三层路由和三层交换机区别有哪些？三层路由有什么特点?
1）功能不同：虽然三层交换机和路由器都具有路由功能，但它们的主要功能并不相同。路由器主要负责实现不同网络之间的通信和数据包的转发，而三层交换机在二层交换的基础上增加了路由功能，能够更高效地处理同一网络内的通信。
2）工作层次：三层交换机可以同时工作在二层和三层，而路由器是三层设备。
3）特殊功能：路由器具有一些特殊功能，如防火墙功能等，这些功能是三层交换机无法取代的。

31、ceph分布式存储
Ceph FS（Ceph 文件系统）确实提供了 POSIX 兼容的文件系统接口，以目录树的形式展示，因此在操作上和直接在 Linux 上操作类似。Ceph FS 可以作为应用程序和操作系统之间的接口，支持各种 shell 命令等。

Ceph RGW（Ceph 对象网关）提供了伪目录的对象存储。用户（user）是对象存储的使用者，通常一个用户只能创建 1000 个存储桶（bucket）。存储桶是用于管理对象（object）的容器，对象可以是文档、图片、视频文件等。尽管用户可以直接上传一个目录，但 Ceph 并不按目录层级结构保存对象，而是将所有对象扁平化地保存在存储桶中。Ceph RGW 提供 RESTful API 接口，支持类似 HTTP POST、PUT、GET 的方式进行操作。可以理解为 Ceph RGW 类似于一个 HTTP 服务器，提供 PUT、GET 方法访问。

Ceph RBD（Ceph 块设备）是 Ceph 提供的块存储功能。例如，一个 4TB 的 RBD 的功能和一个 4TB 的 SATA 相似，挂载的 RBD 就可以当作磁盘使用。从用法和用途上来看，Ceph RBD 相当于一块硬盘挂载到本地，与硬盘用途相同。

32、tcp协议是否也像http协议那样处理cookie
TCP协议和HTTP协议位于不同的网络层级。TCP协议位于传输层，而HTTP协议位于应用层。
HTTP协议建立在TCP协议基础之上。TCP协议主要负责数据传输的可靠性、有确认、重传、数据排序、流量控制和拥塞控制。
而Cookie是在HTTP协议中使用的，用于保存浏览器和服务器之间的状态信息。因此，TCP协议并不像HTTP协议那样处理Cookie。

33、我把服务器的windows firewall服务停止掉了，然后远程桌面就突然断开了？
在某些情况下，当 Windows 防火墙服务被禁用时，远程桌面（RDP）功能可能会失效。
所以建议在远程情况是优先调整策略而不是关闭操作。

34、为什么路由器共用ip模块而每个网卡都有独立的arp模块？
路由器共用IP模块，而每个网卡都有独立的ARP模块，这是因为ARP（地址解析协议）的主要作用是在同一个局域网内确定IP地址与MAC地址之间的对应关系。

通俗地说，IP地址是网络设备在互联网上的地址，类似于我们的家庭住址；而MAC地址则是网络设备的唯一硬件标识，类似于我们的身份证号。
在同一个局域网内，设备之间进行通信时需要知道对方的MAC地址。而ARP协议就是帮助我们在已知目标IP地址的情况下，找到对应的目标MAC地址。

路由器具有多个网络接口，每个接口都连接到不同的网络。在不同的网络中，每个网络接口都需要与所在网络内的其他设备进行ARP通信，以找到IP地址和MAC 地址之间的映射关系。因此，每个网络接口都需要一个独立的ARP模块来处理这些映射关系。

举一个简单的例子，假设我们有一个路由器，它有两个网卡分别连接到两个不同的局域网。每个局域网内的设备需要与路由器的相应网卡进行通信。由于每个局域网内的设备可能具有不同的IP和MAC 
地址，所以每个网卡需要有一个独立的ARP模块来处理与所连接网络内设备之间的IP和MAC地址映射关系。

35、java的继承和接口 有什么区别
继承可以获得父类的属性和方法，但接口只提供了方法的声明。
一个子类只能继承一个父类，但一个类可以实现多个接口。
子类可以覆盖或重写从父类继承来的方法，但必须实现接口中定义的所有方法。
继承是一种 "is-a" 关系，即子类是父类的一种特殊类型，而接口是一种 "has-a" 关系，即一个类 "has-a" 一些行为或功能。
继承关系在编译时确定，而接口实现关系在运行时确定。

继承和接口都是实现代码重用的有效方式，但在不同的情况下使用。如果您需要在不同的类之间共享代码和状态，可以使用继承。
如果您需要定义一组规范和约束，以确保类实现了必要的方法，可以使用接口。

36、


37、


38、


39、


40、












